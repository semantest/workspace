{
  "name": "chatgpt-browser-extension",
  "version": "1.0.0",
  "description": "ChatGPT Browser Extension with enhanced features",
  "main": "background.js",
  "scripts": {
    "build": "mkdir -p dist && cp manifest.json dist/ && cp *.js dist/ && cp *.html dist/ && cp *.css dist/ 2>/dev/null || true",
    "build:webpack": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint *.js src/**/*.js || echo 'No JS files to lint'",
    "lint:fix": "eslint *.js src/**/*.js --fix || echo 'No JS files to lint'",
    "package": "npm run build && cd dist && zip -r ../chatgpt-extension.zip .",
    "package:store": "npm run build && cd dist && zip -r ../chatgpt-extension-store.zip . -x '*.map'",
    "validate": "node -e \"const m=require('./manifest.json'); console.log('✅ Manifest valid:', m.name, 'v' + m.version)\"",
    "clean": "rm -rf dist *.zip"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "copy-webpack-plugin": "^11.0.0"
  },
  "keywords": [
    "chrome-extension",
    "chatgpt",
    "ai",
    "browser-extension"
  ],
  "author": "Your Name",
  "license": "MIT"
}