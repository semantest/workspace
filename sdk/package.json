{
  "name": "@semantest/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Semantest SDK monorepo",
  "workspaces": [
    "core",
    "contracts",
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
