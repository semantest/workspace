// Test setup for {{DOMAIN_NAME}} domain module
import { jest } from '@jest/globals';

// Global test setup
beforeAll(() => {
  // Setup code before all tests
});

afterAll(() => {
  // Cleanup code after all tests
});

beforeEach(() => {
  // Setup code before each test
  jest.clearAllMocks();
});

afterEach(() => {
  // Cleanup code after each test
});

// Mock external dependencies
jest.mock('@semantest/core', () => ({
  // Mock core module exports
}));

// Test utilities
export const createMock{{MAIN_ENTITY_CLASS}} = () => ({
  // Mock entity
});

export const createMock{{MAIN_VALUE_OBJECT_CLASS}} = () => ({
  // Mock value object
});